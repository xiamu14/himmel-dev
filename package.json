{
  "description": "",
  "scripts": {
    "amend": "pnpm -r --filter=himmel run build && pnpm add himmel -r --filter demo",
    "demo:dev": "pnpm -r --filter=demo run dev",
    "release": "cd packages/core && npx renew-it"
  },
  "keywords": [],
  "author": "ben",
  "license": "ISC",
  "volta": {
    "node": "18.17.1"
  },
  "dependencies": {
    "cli-color": "^2.0.3",
    "immer": "^10.0.2"
  },
  "devDependencies": {
    "eslint-config-alloy": "^5.1.2"
  }
}
